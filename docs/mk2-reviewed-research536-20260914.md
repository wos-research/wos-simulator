Controlled research checkpoint:536 reports

The current catalogue19 normal CLI executed all536 distinct reviewed cases from85 complete fixture files. Results:516 exact,20 unresolved,517 complete survivor outcomes matching,535 correct observed winner classifications,3216 survivor slots checked and1324 explicit chance counters checked. There were107 known warnings and zero execution errors. No matching filter, disabled-case exclusion, fitted input or private-candidate substitution was used. The separate external inbox is outside this explicitly enumerated research corpus.

All526 previously reviewed comparison rows are preserved unchanged. The ten additions are five changed-context reverse T7 reports and five changed-context reverse asymmetric Infantry reports. All ten remain unresolved by installed catalogue19. Their independently frozen private model results are provided separately and are not included in installed accuracy. The UI correction reduces private reverse Infantry A/I full matches to4/5 while their survivor quantities and proc counts remain5/5; the reverse T7 candidates remain5/5 and do not distinguish their timing policies.

Winner correctness here means agreement with the fixture's observed winner classification; it does not imply independent raw server winner fields. The fourth new reverse Infantry report has both survivor totals zero. Independent full report UI shows attacker defeat and defender victory, so the corrected v2 observed winner is defender. Its historical survivor-derived draw is preserved separately and explicitly superseded. The raw combat payload itself contains no independent winner field. All536 cases were rerun against the corrected oracle; no battle inputs, seeds, survivors or counters changed.

Every fixture file was copied byte-for-byte. Complete sanitized combat sidecars were retained, including all casualties, kills, power losses, statistics, buffs, experts and troop profiles. Existing sidecars and both new packages were preserved without rewriting. Seeds are original recorded packet values; independently captured timestamps and legacy adapter timestamp origins remain distinct evidence. These changed-context reverse controls do not establish original-context confirmation or uniquely distinguish the candidate timing policies.

This checkpoint describes the controlled reviewed research corpus. The accompanying corpus manifest enumerates every testcase file and ID; the consolidated publication manifest covers that corpus, existing full combat sidecars and the additive evidence/aggregate files. Historical manifests remain intact. Digests use UTF-8 LF normalization with original source-byte digests stored separately.
