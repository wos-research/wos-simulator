# Fresh forward Infantry evidence and current541 check

The current catalogue19 normal CLI executes all541 controlled reports: 516/541 exact, 539/541 correct winner classifications, 25 unresolved, 0 errors and 107 warnings. All cases stay in the denominator. The prior536 complete comparison rows are unchanged. This is controlled evidence, not external inbox accuracy.

The five new reports use100 T10 FC5 Lancers attacking1 T10 FC5 Infantry plus100 T10 FC5 Lancers with exact independently captured modifiers, experts and empty heroes. Frozen attacker-first candidate A matches5/5. Infantry-side-first I matches winners and survivors but assigns Ambusher counts incorrectly in all5. H0 matches0/5 fully and4/5 winners. Candidate results are separate from installed accuracy. Installed full battle results, native RNG and traces equal frozen H05/5 after only version19-to17 label normalization.

Five UI-bound winners,30 survivor slots,25 explicit counters, original packet seeds and complete sanitized decoded combat payloads are included. Only formation identity is replaced by a side-local ordinal. Independent capture times remain in replay.timestamp with reportedSeed override; the old derived adapter time is separately labelled. All five observed seed/time differences are+1; this is descriptive, not a gate.

The controller saw operational UI. The analyst incidentally read the four confirmation UI summaries from combined provenance before packet release; no held-out packet seed/proc or model replay preceded that release. This limitation is preserved. Predicted traces do not establish physical server RNG timing. All frozen numerical algorithms and all prior failures remain unchanged.
