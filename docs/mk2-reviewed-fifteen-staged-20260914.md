# Fifteen newly reviewed controlled reports: staged evidence

These fifteen reports add zero exact matches and fourteen correct winners against installed catalogue18. Frozen private candidates match all fifteen within the exact exported scopes. This private evidence package includes no kernel change or candidate installation.

| Cohort | Reports | Active exact | Active correct winners | Frozen private exact |
|---|---:|---:|---:|---:|
| Next210, mixed defender | 5 | 0 | 5 | G1 5; F1 0 |
| Next210, mixed attacker | 5 | 0 | 5 | G1 5; F1 5 |
| Own Infantry250, forward | 5 | 0 | 4 | Cached 5; controlled reference 5; full reference 0 |

The carried-forward reviewed total would be511:496 exact,15 pending and510 correct winners against catalogue18. That arithmetic carries forward the earlier installed496 validation; no full511 rerun is claimed here. The external inbox and older timestamp exceptions remain unchanged. Own-Infantry reverse reports are excluded pending separate review.

Next210 uses100 T5 FC5 Lancers against10 T10 FC5 Lancers plus100 T5 FC3 Marksmen in both roles. Forward outcomes distinguish the frozen G1 and F1 candidates. Reverse results confirm role/accounting behavior: full numerical traces and RNG values/order agree, while diagnostic phase labels differ. This does not independently establish physical draw placement.

Own Infantry250 uses25 T10 FC5 Infantry plus100 T10 FC5 Lancers per side. The cached candidate changes Ambusher draw reservation while retaining original side-local normal slots and effect triggers. The controlled reference preserves those normal slots and matches it; full reference changes scheduling and fails every complete report comparison. Its first case matches survivors but misses two counters. H0 and full reference predict the wrong winner in the fifth case. All failures remain in the comparison files.

Each fixture preserves its original recorded packet seed, independently linked mail/march timestamp, six survivor slots, every explicit supported chance count including zeros, exact normalized stats, raw modifiers, expert records and troop profiles. Replay supplies the recorded seed explicitly. The old seed-minus-one adapter timestamp is retained separately as provenance. All fifteen time-plus-one relations pass descriptively; they were never used to adjust a seed or select or discard a report.

The three sanitized report sidecars retain every decoded combat field, including casualties, kills, power losses and complete statistics. Only participant formation identity is replaced by the disclosed side-local ordinal1. Private report/mail identifiers, player names and UIDs, coordinates, device/session details and capture envelopes are excluded. Each independent report has its own public test ID; identical combat payloads are never deduplicated. Raw battle-payload and source-artifact hashes retain provenance without publishing private paths. Exact seeds and times can still permit linkage with outside records; anonymization does not imply guaranteed unlinkability.

Validation independently replays all fifteen exported fixtures through installed catalogue18 and compares complete outputs, traces and native RNG against the frozen H0 archives. All fifteen agree. For the five own-Infantry archives, only the catalogue version label is normalized from18 to17; no numerical, RNG or trace fields are changed. Independent source checks cover15 seeds,90 survivor slots,70 explicit chance counters and360 stat axes. Original source and dependency hashes are checked before and after export replay.

The candidate models were frozen before the corresponding outcome releases. Each batch opened one chronological diagnostic before four confirmations. Operational report summaries were seen during collection; model-level seed, proc and outcome reads followed the explicit releases. No fitting of seeds, statistics, probabilities or scheduling was performed. Complete predicted traces are retained privately; matching observations does not uniquely prove unobserved server internals or support arbitrary contexts.

The expected-report coverage manifest enumerates all fifteen fixture paths and hashes. Reproduce active fixture comparisons with the repository testcase CLI, matching each of the three cohort prefixes. Fifteen mismatches are expected against this unchanged catalogue18 baseline. A later reviewed cohort should be added through new files and a successor coverage manifest, preserving this package unchanged.
